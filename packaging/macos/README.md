# MUNI lab macOS app icon

- `MUNILab.icns`: ready for a macOS application bundle's `CFBundleIconFile`.
- `MUNILab.xcassets/AppIcon.appiconset`: Xcode asset catalog covering 16–1024 px.
- `MUNILab.iconset`: source iconset used by `iconutil`.

The vector source is `web/ui/public/muni-lab-icon.svg`.
