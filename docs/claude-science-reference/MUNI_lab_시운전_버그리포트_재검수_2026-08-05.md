# MUNI lab 시운전 버그리포트 재검수 — 2026-08-05

- **검수 시각**: 2026-08-05 07:15–07:40 KST
- **검수 대상 문서**: `artifacts/muni-lab-trial-run-2026-08-04.md` (시운전 20:39–21:07)
- **재검증 방법**: 라이브 UI(snapshot) + localStorage 감사 + 코드/라우트 확인 + git 상태 대조
- **라이브 상태**: `127.0.0.1:5173` dev, `127.0.0.1:4173` 빌드 프리뷰, 설치 앱 `65202`
- **HEAD**: `feat/mucha-science-live-web-research` @ `76e092d` (시운전 이후 신규 커밋 없음, 수정은 미커밋 작업 트리)

## 0. 총평

기존 리포트의 QA-001~008는 **근거가 정확하고 재현 경로가 유효**하다.  
그러나 **리포트에서 빠진 결함 4건**(제목 중복, 아티팩트 라이브러리 부재, 이중 셸, HITL 회복 경로 부재)이 라이브 재검수에서 확인됐고, 그중 2건은 High다.  
시나리오 11개 중 6개(5/6/8 FAIL, 1/2/9 BLOCKED)는 여전히 미종결 상태다.

---

## 1. 기존 결함 재검증 상태

| ID | 제목 | 심각도 | 리포트 상태 | 재검수 상태 | 근거 |
|---|---|---|---|---|---|
| QA-001 | 설치 앱에 연구 백엔드 없음 | Blocker | 미수정 | **여전히 오픈** | `websocket_server.py:36-41` ALLOWED_ORIGINS가 여전히 4173/5173만 허용, 설치 앱 랜덤 포트 미포함 |
| QA-002 | 재연결 실패로 프론트 죽음 | High | 수정 확인 | **유지(수정)** | `useResearchPipelineBridge.ts` detach + `useResearchConversation.ts` catch |
| QA-003 | 설정 왕복 시 미제출 질문 소실 | Medium | 수정 확인 | **유지(수정)** | `ResearchConversationPage.tsx` draft 보존 |
| QA-004 | 실패 턴 "실행 투영을 기다리는 중" | Medium | 수정 확인 | **유지(수정)** | `ResearchConversationTurn.tsx:32-40` |
| QA-005 | 수정 요청 문구 미전달 | High | 미수정 | **여전히 오픈** | `useResearchPipelineBridge.ts` action에 comment 미포함 |
| QA-006 | 보고서 페이지 연결 부재 | High | 미수정 | **여전히 오픈** | `App.tsx:88-97` `/report/:runId` 존재, 대화에서 미연결 |
| QA-007 | 랜덤 포트 origin 저장소 분리 | High | 미수정 | **여전히 오픈** | localStorage 기반, 포트별 origin 격리 지속 |
| QA-008 | compact 링크 a11y 이름 소실 | Low | 미수정 | **여전히 오픈(낮은 우선)** | 라이브 rail에서 compact 상태 확인 |

## 2. 리포트에서 빠진 항목 (GAP)

### GAP-M1 (High) — 세션 제목 전부 프롬프트 전문, 레일에서 9개 중복

- 라이브 실측: 레일 "저장된 연구 대화"에 **동일한 프롬프트 전문 버튼 9개**가 중복 표시. localStorage 감사에서도 9개 세션 전부 같은 제목 문자열.
- 코드: `createThreadLabel`이 `researchConversation.ts:9`에 import만 되고 **summaries/rail 타이틀에 사용되지 않음**. `createConversationSummary`는 `firstPrompt`를 그대로 title로 사용 (`researchConversationStorage.ts`).
- 리포트 미기재: QA-003 draft 보존을 다뤘지만 **제목 압축/식별 문제는 아예 없음**.
- 영향: 사용자가 세션 구분 불가. CS 이식 명세 §5.2의 P0-2와 직결.

### GAP-M2 (High) — 성공 실행 산출물 2개가 존재하나 Library/뷰어 없음, "Live artifacts" 허상

- 라이브 실측: 성공 세션(`session-bae42855`)은 `artifactIds: 2`(report, source-audit) + finalReport 있음.
- 그러나 UI에 라이브러리/뷰어 라우트 없음. 레거시 Sidebar의 **"⌁Live artifacts"** 링크 → `/browser` → `/scientific` redirect 허상.
- 리포트 미기재: QA-006이 "보고서 페이지 연결"만 다루고 **아티팩트 라이브러리 자체의 부재**는 다루지 않음.
- 코드: `App.tsx:91` `/browser` redirect, `Sidebar.tsx`에 "Live artifacts" 잔존.

### GAP-M3 (Medium) — 이중 셸: scientific ↔ 레거시 Sidebar 셸 전환

- 라이브 실측: scientific 셸에서 "실행 설정" 클릭 → `#/` 루트의 **레거시 Sidebar 셸**(설정 화면)로 전환. 셸·내비·스타일이 완전히 다른 제품처럼 보임.
- 레거시 셸에 "Browser" 링크, "◷Scheduled"·"▣Dispatch 베타" 비활성 텍스트 잔존.
- 리포트 미기재: QA-003(설정 왕복)에서 설정으로 이동은 했지만 **셸 전환 자체를 결함으로 보고하지 않음**.
- 영향: 사용자 이동감 상실, 죽은 내비 잔존. CS 이식 명세 §5.3 단일 셸과 직결.

### GAP-M4 (High) — HITL changes_requested 재게이트 실패 시 회복 경로 부재

- 코드 확인: `src/runtime/live_mode.py` 무변경 — `assert_live_hitl`은 `approved` 외에 `LiveModeViolation`을 던짐.
- `src/pipeline/idea_to_council.py`는 evidence gate에서 **1회** revision 후 재게이트, 실패 시 예외 종료.
- `ResearchConversationTurn.tsx`에 recovery UI 없음 (재승인/재개/포크 버튼 부재).
- 라이브 실측(8/4): 9개 실행 중 8개가 error, 실패 문자열 `live mode requires approved HITL gate 'evidence'; got 'changes_requested'`.
- 리포트 미기재: QA-005가 "수정 문구가 재검색에 전달 안 됨"만 다루고, **재게이트 실패 → 하드 페일 → 사용자 회복 불가**의 별개 결함은 다루지 않음. #6 시나리오 FAIL로 일부 표면화됐지만 결함 ID가 없음.

### GAP-M5 (Low, 확인 필요) — 설정 화면 OpenCode Go API Key 필드에 값 존재

- 라이브 실측: `#/` 설정의 OpenCode Go API Key 입력에 값이 채워져 있음(snapshot상 마스킹되어 값 성격 판별 불가).
- 리포트는 "가짜 키는 즉시 제거했다"고 기록 → **모순 소지. 사용자 확인 필요** (실제 키를 나중에 입력했을 수도, 세션 저장 잔재일 수도). 값은 절대 노출하지 않음.

### GAP-M6 (Info) — Python 전체 스위트 4 failed 잔존이 우선순위에 없음

- 리포트 검증 게이트에 기록됐지만 "다음 우선순위"에 미포함. `test_pipeline_runner`·`test_runtime_paths`·sidecar 빌드 2건 — 별도 트래킹 필요.

## 3. 리포트에서 확인된 것 (누락 아님, 유지)

- 5/6/8 FAIL의 근본 원인이 QA-001 하나로 수렴한다는 분석은 정확.
- 1/2/9 BLOCKED가 LaunchServices/launchd 권한 제한 때문이라는 판정은 이 샌드박스 한정으로 정확.
- "다음 우선순위" 6항목 자체는 타당.

## 4. 시나리오 커버리지

| # | 시나리오 | 시운전 판정 | 재검수 판정 |
|---|---|---|---|
| 1 | Spotlight 실행 | BLOCKED | 사용자 환경 재확인 필요 |
| 2 | 중복 실행 | BLOCKED | 사용자 환경 재확인 필요 |
| 3 | 기본 연구 화면 | PASS | 유지 |
| 4 | 자격 증명 없는 실행 | PASS | 유지 |
| 5 | Quick 과학 질문 | FAIL | **QA-001 해결 후 재시운전 필요** |
| 6 | 수정 요청 후 재검색 | FAIL | **QA-005+GAP-M4 해결 후 재시운전** |
| 7 | 공급자 타임아웃 | PARTIAL | 재시운전 필요 |
| 8 | 최종 보고서 | FAIL | **QA-006+GAP-M2 해결 후 재시운전** |
| 9 | 앱 종료 수명주기 | BLOCKED | 사용자 환경 재확인 필요 |
| 10 | 재실행 및 세션 보안 | PARTIAL | QA-007 해결 후 재검증 |
| 11 | 작은 화면 | PASS | 유지 |

미종결 7개 중 사용자 환경 3개(1/2/9) + 백엔드 연동 4개(5/6/7/8).

## 5. 권고 우선순위 (기존 리포트 + GAP 반영)

1. **P0** macOS bundle sidecar 통합 (QA-001) — 5/6/8 재시운전의 선행 조건
2. **P0** GAP-M4: HITL soft-fail + recovery UI (재승인/재개/포크) — 8/9 실패 세션의 직접 원인
3. **P0** GAP-M1: 세션 제목 압축(createThreadLabel 연결) — 라이브에서 즉시 확인 가능한 결함
4. **P0** GAP-M2: Artifact Library 라우트 + "Live artifacts" 허상 제거
5. **P1** QA-005: HITL comment를 action schema와 plan에 반영
6. **P1** QA-006: 대화 → `/report/:runId` 연결
7. **P1** QA-007/GAP-M3: 단일 고정 origin 또는 native persistence + 단일 셸 정리
8. **확인** GAP-M5: OpenCode Go 키 필드 잔존 값 확인
9. **트래킹** GAP-M6: Python suite 4 failed 분리 관리

## 6. 증거 인덱스

| 주장 | 근거 |
|---|---|
| 제목 중복 9개 | 라이브 rail snapshot + localStorage 감사 (2026-08-05 07:15) |
| artifactIds=2, 뷰어 없음 | localStorage 감사 + App.tsx 라우트 + Sidebar "Live artifacts" |
| 이중 셸 | "실행 설정" 클릭 → 레거시 셸 전환 라이브 확인 |
| HITL 하드페일 | live_mode.py 무변경 + ResearchConversationTurn recovery 부재 + 8/9 error 세션 |
| QA-001 유지 | websocket_server.py ALLOWED_ORIGINS 4173/5173 |
| 미커밋 상태 | git status (시운전 수정 전부 working tree) |

*이 문서는 시운전 리포트의 누락을 채우는 검수본이다. 원본 리포트와 함께 보관.*
